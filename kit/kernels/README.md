# kernels/

Fail-closed organs. SOFTWARE, not trained weights. Canonical sources:

- [szl-holdings/szl-khipu](https://github.com/szl-holdings/szl-khipu) — yarqa, maskmod, receipt_attn, ouroboros, block_kv, lambda_gate, governed_norm, formulas, chain, doctrine
- [szl-holdings/szl-nemo](https://github.com/szl-holdings/szl-nemo) — `szl_nemo/rules.py` `rule_check()` R1–R5

`chain.py` is a SHA-256 silhouette of the metal SHA3-256 kernel. Same receipt shape, different digest alg — labeled, not faked. `doctrine.py` is frozen v11 facts (749/14/163, Conjecture 1 OPEN, proven_trust false).

A sklearn joblib may exist historically on Hugging Face `SZLHOLDINGS/szl-nemo`. It is **quarantined**. Do not load it. `rule_check()` is the approved path.

```python
from kernels.nemo_rules import rule_check
ok, violated = rule_check("how good is this?", "accuracy 99%")
# ok is False, violated includes R1_no_fabrication_label
```

Λ uniqueness remains Conjecture 1 OPEN. Energy UNAVAILABLE. Apache-2.0.
